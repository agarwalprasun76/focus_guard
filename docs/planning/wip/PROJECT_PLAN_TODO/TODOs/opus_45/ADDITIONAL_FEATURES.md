Additional Features:
    1) Add a module to save links that are getting blocked during focus activity (ask user to add a comment on why they wanted to watch it, save time and link in a database and allow user to watch it during break time)
    2) User request/ user feedback collation module
    3) Improve agentic approach to debugging by reviewing logs and fixing issues.
    4) can we create multiple VMs and run FocusGuardService on each VM and test the application by having some agent do random things to generate logs and usage patterns
    5) On the dashboard/ popup add a link to display the websites that were blocked and number of times they were blocked.
    6) Check why the focus  guard icon is not showing in the tray is not working
