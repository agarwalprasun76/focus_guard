# Plan 02/15/2026 — Next Steps (Post Review)

## Inputs Reviewed
- `docs/planning/wip/PROJECT_PLAN_TODO/TODOs/gpt_53_codex/PROGRESS_TRACKER.md`
- `docs/planning/wip/PROJECT_PLAN_TODO/TODOs/gpt_53_codex/POST_P4_EXECUTION_TASK_BOARD.md`
- `docs/planning/wip/PROJECT_PLAN_TODO/TODOs/gpt_53_codex/POST_P4_INTEGRATION_AND_TEST_STRATEGY.md`
- `docs/planning/wip/PROJECT_PLAN_TODO/TODOs/gpt_53_codex/LOOPHOLE_TRACKER.md`
- `docs/planning/wip/PROJECT_PLAN_TODO/TODOs/gpt_53_codex/P4_08_RELEASE_GATE_DECISION.md`
- `docs/planning/wip/PROJECT_PLAN_TODO/TODOs/opus_45/PROGRESS_TRACKER.md`
- `docs/planning/wip/PROJECT_PLAN_TODO/TODOs/opus_45/DEPLOYMENT_AND_MVP_PLAN_02062026.md`

---

## Current State (Synthesis)
1. **Phase 1 Admin UX (P1-P4) is implemented and validated**, including release gate GO and post-P4 I0-I6 execution closeout.
2. **Post-P4 next recommended work is already identified**: close deferred loopholes (`L-003` by 2026-02-20, `L-002` by 2026-02-22) and run Shadow Cycle 002.
3. **Runtime contract hardening is in progress in code** (tab-server endpoint centralization + config-driven contract), but this needs full packaged-lane verification and tracker updates.
4. **opus_45 track is historically complete for A-E (deployment v1.0), with roadmap items as future work**; near-term effort should avoid reopening already-closed A-E tasks and instead consume relevant roadmap items only when they align to current gpt_53_codex priorities.

---

## Prioritized Next Steps

## P0 (Immediate: today)

### P0-1. Runtime contract verification pass (source + packaged)
**Goal:** Eliminate frontend/backend/runtime drift after tab-server endpoint centralization.

- Verify all critical endpoints and paths in both lanes:
  - `/admin/health`
  - `/admin/api/v1/meta`
  - `/admin/api/v1/dashboard`
  - `/admin/api/v1/devices`
  - tab-server health endpoint from current deployment config host/port
- Re-run packaged smoke profile and packaged runtime verifier.
- Confirm `/admin` SPA routing, auth flow, and devices/dashboard refresh behavior from a clean launch.

**Deliverables**
- Evidence block added to `PROGRESS_TRACKER.md` (commands + pass/fail).
- Any mismatch logged to `LOOPHOLE_TRACKER.md` with risk scoring.

---

### P0-2. Deferred loophole closure window start (L-003 first)
**Goal:** Execute dated follow-up commitments from I6.

- Resolve/validate `L-003` packaged mutation confidence gap first (higher risk score and earlier revisit date).
- Expand packaged smoke to include mutation assertions (create/revoke and enforcement mutation confidence checks).

**Deliverables**
- Updated loophole status (`deferred` -> `in_progress`/`fixed`/`verified`).
- Test coverage link + run evidence in tracker.

---

### P0-3. Debug override-expiry enforcement mismatch (YouTube 1-minute case)
**Goal:** Determine whether the bug is in enforcement logic, popup messaging, or test harness assumptions.

- Reproduce exact scenario in both source and packaged lanes:
  - grant temporary override for YouTube for 60 seconds
  - verify effective allowed period
  - verify re-block behavior immediately after expiry window
- Trace and compare:
  - override grant/active/expired state from tab-server APIs and logs
  - frontend popup/status messaging timing vs backend enforcement timing
  - simulation/manual harness assertions for expiry semantics
- Decide root cause category:
  - backend enforcement bug
  - UI message/state bug
  - harness/test expectation bug
- Implement fix and add regression coverage for the exact 60-second expiry path.

**Deliverables**
- Root-cause note with timestamps and endpoint/log evidence.
- Linked fix + regression test(s) in tracker.
- Loophole tracker entry updated with final status and rationale.

### P0-4. Debug hourly email report blank-content issue
**Goal:** Determine why hourly email reports are sent without expected detail content.

- Reproduce with a controlled data window:
  - generate known activity/block/override events before the report window closes
  - force or wait for hourly report generation
  - capture generated payload/content before send
- Trace and compare pipeline stages:
  - report data aggregation source(s)
  - template/rendering layer output
  - SMTP/send path payload
- Decide root cause category:
  - aggregation/data selection bug
  - rendering/template bug
  - scheduling/windowing bug
  - delivery formatting bug
- Implement fix and add regression coverage for hourly report content presence and key sections.

**Deliverables**
- Root-cause note with timestamps and evidence from logs/report payloads.
- Linked fix + regression test(s) in tracker.
- Tracker entry updated with final status and rationale.

---

## P1 (Next 2-5 days)

### P1-1. Run Shadow Cycle 002 with new nightly data
**Goal:** Continue policy-driven closure and avoid premature rule promotion.

- Run next nightly simulation cycle(s).
- Produce `I6_SHADOW_RULE_CYCLE_002.md` using existing scorecard format.
- Keep promotion gate strict: no active promotion unless evaluable signal quality is sufficient.

**Deliverables**
- New cycle doc + explicit go/no-go.
- Top-5 queue refreshed in `LOOPHOLE_TRACKER.md`.

---

### P1-2. Complete L-002 revisit and decision
**Goal:** Close or re-defer recovery-message responsiveness issue with explicit evidence.

- Collect live-session observations and e2e evidence from current polling/retry behavior.
- Decide: close, tune polling/retry UX, or defer again with rationale and date.

**Deliverables**
- Updated `LOOPHOLE_TRACKER.md` and `I6_BACKFILL_BASELINE.md` references.

---

## P2 (Housekeeping / Governance)

### P2-1. Tracker consistency cleanup pass
**Goal:** Remove status ambiguity across planning artifacts.

- In `gpt_53_codex/PROGRESS_TRACKER.md`, align top-level phase summary row for P4 with completed checklist state.
- Ensure "Last Updated" headers reflect latest session date where relevant.
- Add a short "Current Runtime Contract" note (tab_server_host/tab_server_port source of truth = deployment_config + shared helper).

**Deliverables**
- Single consistency update commit for trackers/docs only.

---

### P2-2. Cross-track alignment note (opus_45 vs gpt_53_codex)
**Goal:** Prevent duplicate planning streams.

- Add one paragraph in gpt tracker clarifying:
  - opus_45 = historical deployment baseline + long-range roadmap context.
  - gpt_53_codex = active execution lane for post-P4 integration/testing and loophole triage.

**Deliverables**
- Reduced coordination ambiguity for future sessions.

---

## Validation Matrix for This Plan

### Minimum must-pass commands
- Backend targeted tests (auth/origin/spa/meta/devices/dashboard as affected)
- Frontend integration tests
- Playwright critical + packaged smoke profile
- Nightly simulation dry-run (for shadow-cycle input)
- Override-expiry regression test path (60-second YouTube override re-block behavior)
- Hourly email report regression path (non-empty expected sections in generated report)

### Exit criteria for this plan cycle
- `L-003` moved out of deferred with evidence-backed state.
- Override-expiry root cause identified and fixed/tested.
- Hourly email blank-content root cause identified and fixed/tested.
- `L-002` revisit completed by target date with explicit decision.
- Shadow Cycle 002 published.
- Trackers synchronized (status/date/next-resume pointer).

---

## Suggested Execution Order (Practical)
1. P0-1 runtime contract verification (source + packaged)
2. P0-2 L-003 packaged mutation gap closure
3. P0-3 override-expiry enforcement mismatch debugging and fix
4. P0-4 hourly email report blank-content debugging and fix
5. P1-1 Shadow Cycle 002
6. P1-2 L-002 revisit decision
7. P2-1/P2-2 tracker and cross-track governance cleanup
