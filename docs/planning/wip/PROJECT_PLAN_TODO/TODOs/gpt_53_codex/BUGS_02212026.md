# FocusGuard Bug Tracker

**Last Updated**: February 22, 2026

---

## Original Reports (Feb 21, 2026)

Saving link from the popup page gives an error. Failed to save: disk I/O erro.
> **Status**: FIXED (BUG-006) — write path hardened

Not sure if the email reporting is working or not. 
> **Status**: PARTIALLY FIXED (BUG-007/023) — email reporter query fixed, activity logger wiring fixed (Feb 22), needs rebuild + verify

I am not sure if the edge extension is working properly, the chrome one was blocking youtube but the edge one isnt.
> **Status**: FIXED (BUG-008) — verified parity with Chrome

How do i see the saved links, should they be added to the extension popup page and the system tray popup page?
> **Status**: FIXED (BUG-009) — dashboard card + blocked-popup CTA added

I also noticed that trying to get to shakespeares macbeth on folger.edu blocked it but i could get it elsewhere so I am concerned about the accuracy of the blocking. I would have thought macbeth would be allowed.
> **Status**: IN PROGRESS (BUG-010) — adaptive confidence pipeline implemented

The focus guard hourly report is going out blank.
> **Status**: PARTIALLY FIXED — root cause was activity logger not started in tray-app path. Fixed Feb 22.

FocusGuard Monitor <focusguardapp@gmail.com>
2:00 AM (4 minutes ago)
to me

🖥️ FocusGuard Hourly Report
Machine: NucBox_K8Plus

User: Siyona Agarwal

Period: 2026-02-21 01:00 - 02:00

📊 Summary
Active Time: 0.0 minutes

Sessions: 0

---

## New Reports (Feb 22, 2026)

### BUG-021: Domains page broken — "unable to load domains"
Domains was working but now getting an error "unable to load domains. Probably some wiring issue."
Introduced during Feb 21 settings/domain management wiring changes.
**Status**: NEW — needs debugging

### BUG-022: App activity tab shows no data
The app activity tab was added to the admin portal but shows no activity. Expected to see usage of Cursor, Windsurf etc over the last 5-60 minutes.
**Root cause found**: EnhancedActivityLogger was only started in Windows Service path (service.py), NOT in tray-app path (main.py). Fixed Feb 22 by wiring into main.py startup.
**Status**: PARTIALLY FIXED — needs rebuild + runtime verification

### BUG-023: Email report shows no activity (related to BUG-007/022)
The email report bug still exists in terms of showing no activity at all. Related to BUG-022 — same root cause (activity logger not persisting samples).
**Status**: PARTIALLY FIXED — same fix as BUG-022, needs rebuild + verify
