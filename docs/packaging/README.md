# FocusGuard

A cross-platform application for monitoring user activity and reducing distractions.

## Installation

1. Clone the repository
2. Install dependencies:
   ```bash
   pip install -e .
   ```

## Usage

Run the browser tab detector:
```bash
python demos/browser_integration/get_browser_tabs.py
```
